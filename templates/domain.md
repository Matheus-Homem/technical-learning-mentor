# Domain declarations

<!-- The user's self-declared Domain, per taxonomy node. THE USER OWNS THIS FILE.
     The skill may propose a row but never writes one without asking.

     SPARSE BY DESIGN: there is a row only for what was declared explicitly.
     Everything else resolves by inheritance, or defaults to `developing`.
     Never add rows for descendants that would inherit the same value —
     that is duplication, and duplication is what goes stale.

     Resolution: the effective Domain of a node is the declaration on the
     LONGEST PREFIX of that node that has one. No declaration on any prefix
     means `developing`. See references/knowledge-model.md.

     Values: waived | mastered | developing -->

| node | domain | declared_on | note |
|---|---|---|---|
