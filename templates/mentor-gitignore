# Placed at .mentor/.gitignore during first-run bootstrap by /mentor-map.
# Everything else in .mentor/ is intentionally version-controlled — it's
# the durable record this skill exists to keep. Excluded: the derived panel,
# session-only /mentor-class output, and the narration glossary
# (see references/audio.md).
#
# If `prune_closed_features_on_close` is enabled in profile.md, /mentor-close
# appends one `features/<slug>/` line below per closed feature — never a
# blanket `features/**`, which would block normal tracking of the next open
# feature. See commands/mentor-close.md.

progress.html
features/*/classes/
narration_glossary.json
